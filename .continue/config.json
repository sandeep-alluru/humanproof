{
  "name": "humanproof",
  "version": "0.1.0",
  "schema": "v1",
  "context": [
    {"provider": "file", "params": {"nRetrieve": 20, "nFinal": 5}},
    {"provider": "code"}
  ],
  "docs": [
    {
      "title": "humanproof Architecture",
      "startUrl": "file://./ARCHITECTURE.md",
      "faviconUrl": ""
    }
  ],
  "customCommands": [
    {
      "name": "explain",
      "prompt": "Explain how this part of humanproof works: {input}. Use the module map in AGENTS.md as context.",
      "description": "Explain a part of the codebase"
    }
  ],
  "rules": [
    "This is humanproof — Motor-noise fingerprinting for AI detection in competitive games.",
    "Library code must not use print() — use rich.console.Console.",
    "All public API must have type annotations and docstrings.",
    "Tests use pytest. CLI tests use click.testing.CliRunner."
  ]
}
